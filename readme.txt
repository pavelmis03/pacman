тот, кому я передаю код!

в этом коде реализовано: главное меню и подпункты (инфо о разработчиках,
	 управление в игре, таблица рекордов)

Если ты
    0) для всех
	1) гейм-дизайнер,
	2) программист
	3) тимлид

0)    !!!тебе обязательно нужно поменять путь до текстовых файлов
      я использую модули import и sys
    используемые файлы: our_info.txt, key_info.txt, highscores.txt - должны существовать!!!

1) ты можешь (без вреда для программы) изменять следущие параметры:
    size - размер окна (строка 4)
    punkts_settings - положение пунктов меню в каждом меню (стр. 94, 101, 109)
    text - положение текста (последние два параметра)
    а также параметры шрифтов, цвета текста и фона (не желательно)

2) мое код имеет несколько констант, которые стот заменить на итоговые при финальном объединении:
    size = [1200, 800]
    screen = pygame.Surface(size)
    window = pygame.display.set_mode((size))
    pygame.font.init()
функция drawText выводит содержимое файлов, которые обязяны существовать, возможные проблемы - их отсутствие и вывод таблицы при неправильной записи*
класс меню - основной:
    методы:
        menu - отображение пунктов меню/перерисовка экрана/вывод информации на экран
        render - отображение выделенного цвета
функции mainMenu, ourInfo, keyInfo, records - функции, которые срабатывают при выборе разных пунктов меню, они создают в себе тот же класс и работают с ним
Пункт (играть):
    при выборе этого пунка функции меню завершиет свою работу и продолжается она в основном теле программы
*сохранение таблицы:
name1    score1
name2    score2
...      ...

    
    

3) тебе будут интересно все тоже, что и гейм-дизайнеру, а таже функционал, который описан в части для программиста
